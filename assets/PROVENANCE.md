# Veritas hero image — provenance

`veritas-hero.jpg` is a **decorative, illustrative** asset. It is not a diagram
and does not depict factual system behavior; the architecture authority is the
Mermaid diagrams in `README.md` and `docs/DESIGN.md`.

| field | value |
|---|---|
| asset | `assets/veritas-hero.jpg` |
| model | `gemini-3.1-flash-lite-image` (Google Gemini API, `:generateContent`) |
| date | 2026-09-05 |
| attempts | 1 of 2 allowed |
| dimensions | 1376 × 768 px |
| tokens | 196 in · 1,522 out (1,120 image) |
| approx. cost | ≈ $0.0023 (input $0.25/1M, output $1.50/1M) |
| spend cap policy | ≤ 2 attempts, hard stop < $0.10 per image, google-only — no provider fallback |

**Prompt (abridged):** abstract decorative hero — heterogeneous fragments of
source material pass through transparent glass-like checkpoint gates into
clean ledger/report pages on the right; a few unresolved strands diverge in
muted amber/gray. Explicitly excluded: text, letters, numbers, UI elements,
icons, checkmarks, seals of approval, robots, figures, logos.
